export const metadata = {
  title: 'TapForMenu | Smart NFC Menus',
  description: 'Your menu, just a tap away. Upgrade your hospitality venue with beautiful NFC tags.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
